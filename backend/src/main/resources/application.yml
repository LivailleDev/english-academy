spring:
  application:
    name: english-academy-backend
  datasource:
    url: jdbc:mysql://localhost:3306/english_academy
    username: english_academy
    password: english_academy
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

server:
  port: 8080

app:
  cors:
    allowed-origins: http://localhost:5173,http://localhost:5183
